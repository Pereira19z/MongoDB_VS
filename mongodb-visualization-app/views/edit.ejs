<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Usuário</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; background-color: #f4f4f4; }
        h1, h2 { color: #333; }
        input[type="text"], input[type="email"], input[type="password"], input[type="number"], input[type="date"] {
            width: 100%; 
            padding: 10px; 
            margin: 5px 0; 
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        button {
            padding: 5px 10px; 
            background-color: #4CAF50; 
            color: white; 
            border: none; 
            border-radius: 5px; 
            cursor: pointer; 
        }
        button:hover { background-color: #45a049; }
    </style>
</head>
<body>
    <h1>Editar Usuário</h1>

    <form action="/edit/<%= usuario._id %>" method="POST">
        <input type="text" name="cpf" value="<%= usuario.cpf %>" required placeholder="CPF" pattern="\d{11}" title="Insira um CPF válido com 11 dígitos">
        <input type="text" name="nome" value="<%= usuario.nome %>" required placeholder="Nome">
        <input type="text" name="rg" value="<%= usuario.rg %>" required placeholder="RG">
        <input type="password" name="senha" placeholder="Senha (deixe em branco para não alterar)">
        <input type="email" name="email" value="<%= usuario.email %>" required placeholder="Email">
        <input type="text" name="telefone" value="<%= usuario.telefone %>" required placeholder="Telefone" pattern="\d{10,11}" title="Insira um telefone válido com 10 ou 11 dígitos">
        <input type="text" name="rua" value="<%= usuario.endereco.rua %>" required placeholder="Rua">
        <input type="number" name="numero" value="<%= usuario.endereco.numero %>" required placeholder="Número">
        <input type="text" name="cidade" value="<%= usuario.endereco.cidade %>" required placeholder="Cidade">
        <input type="text" name="estado" value="<%= usuario.endereco.estado %>" required placeholder="Estado">
        <input type="text" name="cep" value="<%= usuario.endereco.cep %>" required placeholder="CEP" pattern="\d{5}-\d{3}" title="Insira um CEP válido no formato 00000-000">
        <input type="date" name="data_nascimento" value="<%= new Date(usuario.data_nascimento).toISOString().split('T')[0] %>" required>
        <button type="submit">Salvar Alterações</button>
    </form>

    <a href="/">Voltar</a>
</body>
</html>
